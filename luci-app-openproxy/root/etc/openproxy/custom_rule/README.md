# Directory Description

This directory is used to store:
- file type rule-provider: Reference configuration template template/rule.yaml

Used to store custom configuration files. The configuration file editor will read all configuration files with the .yaml suffix in the current directory.


## rule-provider configuration

For a complete configuration example, please refer to the template/rule.yaml file.
